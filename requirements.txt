fastapi
uvicorn[standard]
httpx
boto3
stripe==15.1.0
PyJWT
pydantic
python-multipart
google-auth-oauthlib
google-api-python-client
paramiko
redis
pillow
pydub
numpy
opencv-python-headless
yt-dlp==2026.06.09
# faster-whisper removed: 500MB+ (ctranslate2/onnxruntime) causes segfault on
# python:3.11-slim without CUDA. Import is guarded in backend.py (try/except).
# Only needed for local STT, not the serverless API pipeline.
edge-tts
# Skeleton AI short-form pipeline (Seedance i2v + Kling premium upgrade).
# i2v_engine.py imports fal_client at module level.
fal-client
# Verify Tauri's base64-wrapped Minisign updater artifacts before the backend
# advertises or serves them. Keep this explicit rather than relying on an
# unrelated package to install PyNaCl transitively.
PyNaCl==1.5.0
# json-repair: tolerant JSON parser that recovers from LLM output mistakes
# (unescaped quotes inside strings, trailing commas, smart quotes, missing
# delimiters). Used by zerotier_private + alt_history_private + long_form
# routers as a fallback after strict json.loads fails on Grok output.
# PR #147 — Casey hit "Expecting ',' delimiter: line 1 column 560" on a
# valid script that just had an unescaped quote in a narration string.
json-repair
